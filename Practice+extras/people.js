const people = ['ali','amna','bilal','faisal'];
console.log(people);
module.exports = people;

const os = require('os');
console.log(os);
 
const os = require('os');
console.log(os.platform());// Returns the operating system platform
